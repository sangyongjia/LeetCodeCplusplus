class Solution {
public:
    bool isAnagram(string s, string t) {
        std::sort(s.begin(),s.end());
        std::sort(t.begin(),t.end());
        //std::cout<<s<<std::endl;
        return 0==strcmp(s.c_str(),t.c_str());
        
    }
};
